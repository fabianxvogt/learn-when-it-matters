# Roadmap

State: **product/reproducibility delivery / scientific attempt closed as bounded INCONCLUSIVE**

## Now

- Keep the closed scientific attempt **INCONCLUSIVE measurement identification**: no model win, equivalence, inferiority, negative hypothesis, or best-method claim.
- Keep the public browser preview on reviewed product source `5ee67f10aa5bbd368ca100214955c0f0cea71268`; the status-copy delta is generation-guarded and the public URL is live. No new research run, calibration, tuning, or architecture expansion.
- Complete any separate public URL/device QA for the tiny malformed-import status delta; prior valid import/export/save/reload/reopen/reset and retention smoke passed.
- Verify clean build, tests, save/reload, export/import, malformed JSON rejection, pause and cancel responsiveness.
- Run the independent counterexample fixtures and the local repeated-seed runner.
- Review staged source for private data, planning files and generated dumps before any later source update.

## Next

- Any future measured run needs a new explicit owner decision; both pilot authorizations are consumed. Do not retry, retune, cherry-pick, or grow architecture from this result.
- New source needs independent product review before any later public push or deploy.
- Compare the local held-out budget slices with the browser example before making any research claim.
- Review [RESULTS.md](RESULTS.md) and the amended `scripts/run-final-investigation.mjs` measurement semantics; do not call its empty measured-bin result a frontier or negative proof.

## Later

- Keep the tiny recurrent learner local-only; extend it only if the benchmark exposes a live, nontrivial hypothesis worth the added cost.
- Add confidence intervals to the browser summary only if they remain interpretable at browser size.

## Done

- [x] Browser working surface with seeded stream, recurrence/noise controls and update budget.
- [x] Always, periodic, random, pre-label surprise, change-point, RLS and lagged usefulness methods.
- [x] Prediction/action/probe/cost traces; probe forwards, gradients, copies, discarded work and elapsed time.
- [x] Versioned localStorage save and portable JSON export/import with malformed-input rejection.
- [x] CPU-only local repeated-run CLI with train-only bounded tuning, frozen update-budget slices, held-out recurring and never-repeating controls.
- [x] Tiny recurrent learner runs locally; browser linear preview is labeled separately.
- [x] Focused README, docs navigation, MIT license and static hosting metadata.
- [x] Separate final-investigation runner with train-only CPU/wall calibration, strict bins, pairwise componentwise upper-cost ceilings, per-model strict manifests, seed-safe pairing, full execution fingerprints, timing tolerances, replay checks and inconclusive outcomes.
- [x] First registered capped final-investigation attempt at source `1321821191e92a832a03426fb1a2b040ca7f1bc0`; private result retained as inconclusive/non-comparable.
- [x] Train-only `raw-batch-cv-k5-v1` amendment and one consumed K=5 run at source `ecd562791963f535dd33fc0380fb82477a0943a5`; 102 calibration rows, 0 strict held-out rows, and 672 partial ceiling rows. Independent review closed the scientific attempt as bounded inconclusive.
- [x] Browser product paths expose seeded recurrence/noise/budget, prediction/action/probe/cost traces, pause/cancel, persistence/export/import recovery, and a keyboard-accessible chart table; live browser acceptance remains a separate product-review gate.
- [x] Browser async state uses monotonic job generations and immutable per-job config snapshots; stale run/import callbacks cannot replace newer results, reset state, controls, progress, or status. Controller and bounded import-validation probes cover deferred completion, cancellation, overlapping imports, reset, nonfinite and out-of-range input.
- [x] Public browser preview deployed from reviewed source `5ee67f10aa5bbd368ca100214955c0f0cea71268`; malformed-import status correction is current-generation guarded. Public delivery does not change the bounded inconclusive science result.

## Full-v1 acceptance matrix

| Check | Evidence | State |
| --- | --- | --- |
| Predict before label | `src/core.js` freezes prediction and action before `target` is read; core tests cover trace determinism | implemented; verified by the full local suite |
| No same-step residual gating | Surprise uses input innovation; change-point uses prior residual state | implemented |
| Lagged usefulness probe | Candidate from `t-1`, shadow transfer resolved on `t`, evidence affects `t+1+` | implemented |
| Matched accounting | Accepted updates plus probe forwards, candidate gradients, optimizer/matrix ops, copies, discarded candidates, budget-skipped requests and measured CPU/wall ms | implemented; browser trace now exposes the ledger |
| Required controls | Seven methods share paired streams and locked config | implemented |
| Held-out evaluation | Final runner locks measured candidates on training seeds and preserves complete paired held-out rows | implemented; K=5 attempt had 0 strict rows and is inconclusive |
| Repeated seeds + uncertainty | CLI emits per-seed paired deltas and bootstrap intervals | implemented; scientific attempt closed inconclusive |
| Tiny recurrent local path | `src/recurrent.js` is wired into the CLI for held-out and never-repeating repeated runs | implemented; local-only path |
| Persistence/export | localStorage, `lwm.v1` JSON export/import and rejection path | source-complete; prior public smoke passed; tiny status recheck remains separate |
| Browser QA | desktop Chromium + narrow mobile | prior import/export/save/reload/reopen/reset smoke passed; public status/device checks remain separate |
| Release | public Site deployed from reviewed `5ee67f10`; final model-study acceptance remains separate from preview release | ordinary product release complete; science remains bounded inconclusive |
